SELECT TOP 1 MAX(SALARY * MONTHS)+" ",+COUNT(NAME)
FROM EMPLOYEE 
GROUP BY SALARY * MONTHS 
ORDER BY MAX(SALARY * MONTHS) DESC

--------------------------------------------------------------------------------------

SELECT TOP 1 MAX(SALARY * MONTHS),COUNT(NAME)
FROM EMPLOYEE 
GROUP BY SALARY * MONTHS 
ORDER BY MAX(SALARY * MONTHS) DESC
